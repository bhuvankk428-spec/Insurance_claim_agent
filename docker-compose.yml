version: "3.9"

services:
  api:
    build: .
    ports:
      - "3000:3000"
    env_file:
     - gemini-app/.env

    depends_on:
      - ollama

  ollama:
    image: ollama/ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama

volumes:
  ollama_data:
